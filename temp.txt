.navigation-bar {
  width: 72px;
  height: 100vh;
  background-color: var(--color-neutral-bg-2);
  border-right: 1px solid var(--color-neutral-stroke-3);
  display: flex;
  flex-direction: column;
  padding: var(--spacing-s) 0;
  flex-shrink: 0;
}

.nav-header {
  padding: var(--spacing-m);
  display: flex;
  justify-content: center;
  margin-bottom: var(--spacing-m);
}

.nav-logo {
  width: 32px;
  height: 32px;
  display: flex;
  align-items: center;
  justify-content: center;
}

.nav-threads {
  flex: 1;
  overflow-y: auto;
  padding: 0 var(--spacing-s);
}

.nav-thread-item {
  width: 100%;
  padding: var(--spacing-s);
  margin-bottom: var(--spacing-s);
  border-radius: var(--radius-button);
  background: transparent;
  border: none;
  cursor: pointer;
  transition: background-color 0.2s;
  position: relative;
}

.nav-thread-item:hover {
  background-color: var(--color-neutral-bg-3);
}

.nav-thread-item.active {
  background-color: var(--color-neutral-bg-3);
}

.nav-thread-item.active::before {
  content: '';
  position: absolute;
  left: 0;
  top: 50%;
  transform: translateY(-50%);
  width: 3px;
  height: 24px;
  background-color: var(--color-brand-primary);
  border-radius: 0 2px 2px 0;
}

.thread-icon {
  width: 40px;
  height: 40px;
  border-radius: var(--radius-circular);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 20px;
  margin: 0 auto;
}

.thread-info {
  display: none;
}

.nav-footer {
  padding: var(--spacing-m);
  display: flex;
  justify-content: center;
  border-top: 1px solid var(--color-neutral-stroke-3);
}

.nav-settings {
  width: 40px;
  height: 40px;
  border-radius: var(--radius-button);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--color-neutral-fg-2);
  background: transparent;
  border: none;
  cursor: pointer;
  transition: background-color 0.2s;
}

.nav-settings:hover {
  background-color: var(--color-neutral-bg-3);
}
